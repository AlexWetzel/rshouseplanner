const initialState = {

};

const types = {
  
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    
    default:
      return state;
  }
};

export { initialState, types, reducer };
